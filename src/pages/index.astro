---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="The Grace Network — A Network State for Human Flourishing"
  description="Join The Grace Network: a political movement built on AI safety, radical transparency, scientific governance, and universal flourishing. Inspired by the principles of responsible AI development and the network state vision."
  image="https://sethgoldstein.com/party/og-grace-network.svg"
>
  <!-- Navigation -->
  <nav class="party-nav">
    <div class="nav-inner">
      <a href="/" class="nav-logo">
        <svg width="32" height="32" viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="60" cy="60" r="56" stroke="#C8A2FF" stroke-width="2"/>
          <path d="M60 20 L60 100" stroke="#C8A2FF" stroke-width="1.5" opacity="0.4"/>
          <path d="M20 60 L100 60" stroke="#C8A2FF" stroke-width="1.5" opacity="0.4"/>
          <circle cx="60" cy="60" r="12" fill="#C8A2FF" opacity="0.9"/>
          <circle cx="60" cy="60" r="28" stroke="#C8A2FF" stroke-width="1" opacity="0.5"/>
          <circle cx="60" cy="60" r="42" stroke="#C8A2FF" stroke-width="0.75" opacity="0.3"/>
          <circle cx="60" cy="32" r="4" fill="#FFD700" opacity="0.8"/>
          <circle cx="84" cy="48" r="4" fill="#FFD700" opacity="0.8"/>
          <circle cx="84" cy="72" r="4" fill="#FFD700" opacity="0.8"/>
          <circle cx="60" cy="88" r="4" fill="#FFD700" opacity="0.8"/>
          <circle cx="36" cy="72" r="4" fill="#FFD700" opacity="0.8"/>
          <circle cx="36" cy="48" r="4" fill="#FFD700" opacity="0.8"/>
        </svg>
        <span>The Grace Network</span>
      </a>
      <div class="nav-links">
        <a href="/constitution">Constitution</a>
        <a href="/toolkit">Toolkit</a>
        <a href="#platform">Platform</a>
        <a href="#sign" class="nav-cta">Join the Network</a>
      </div>
      <button class="mobile-menu-btn" aria-label="Toggle menu">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
      </button>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-grid"></div>
      <div class="hero-glow"></div>
    </div>
    <div class="hero-content">
      <div class="hero-badge">A NETWORK STATE FOR THE AGE OF INTELLIGENCE</div>
      <h1>
        <span class="hero-line-1">Machines of</span>
        <span class="hero-line-2">Loving Grace</span>
      </h1>
      <p class="hero-subtitle">
        We are building a digital-first political movement founded on the belief that powerful
        technology—developed safely, governed transparently, and distributed broadly—can create
        cascading abundance for every human on earth.
      </p>
      <div class="hero-actions">
        <a href="#sign" class="btn-grace">Sign the Constitution</a>
        <a href="/constitution" class="btn-ghost-grace">Read Our Principles</a>
      </div>
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number" id="memberCount">7</span>
          <span class="stat-label">Pillars of Governance</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-number">1</span>
          <span class="stat-label">Shared Moral Vision</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <span class="stat-number">&infin;</span>
          <span class="stat-label">Potential for Flourishing</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Manifesto Strip -->
  <section class="manifesto-strip">
    <div class="manifesto-inner">
      <p>
        "We do not need to choose between progress and safety, between innovation and equity,
        between the power of technology and the dignity of humanity. We choose all of it."
      </p>
    </div>
  </section>

  <!-- Platform Pillars -->
  <section class="platform" id="platform">
    <div class="section-inner">
      <div class="section-header">
        <span class="section-tag">OUR PLATFORM</span>
        <h2>Seven Pillars of Aligned Governance</h2>
        <p class="section-desc">
          Drawing from the principles of responsible AI development, constitutional governance,
          and the network state vision, our platform charts a course for human flourishing
          in the age of intelligence.
        </p>
      </div>

      <div class="pillars-grid">
        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><circle cx="24" cy="24" r="20" stroke="#C8A2FF" stroke-width="1.5"/><path d="M24 12v24M16 20l8-8 8 8" stroke="#C8A2FF" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <span class="pillar-num">I</span>
          <h3>Safety-First Progress</h3>
          <p>
            We advance powerful technologies through rigorous, responsible scaling. Every leap in
            capability is matched by a leap in safety. We prove systems are safe before we deploy
            them, not after. The precautionary principle is not an obstacle to progress—it is the
            foundation of durable progress.
          </p>
        </div>

        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><circle cx="24" cy="24" r="20" stroke="#FFD700" stroke-width="1.5"/><circle cx="24" cy="24" r="8" stroke="#FFD700" stroke-width="1.5"/><circle cx="24" cy="24" r="3" fill="#FFD700"/></svg>
          </div>
          <span class="pillar-num">II</span>
          <h3>Universal Flourishing</h3>
          <p>
            The benefits of transformative technology must cascade to all—not pool at the top.
            We advocate for policies that ensure AI-driven breakthroughs in health, education,
            and economic opportunity reach every community. Abundance is not a zero-sum game;
            it compounds when shared.
          </p>
        </div>

        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><rect x="8" y="8" width="32" height="32" rx="4" stroke="#C8A2FF" stroke-width="1.5"/><path d="M16 24h16M24 16v16" stroke="#C8A2FF" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <span class="pillar-num">III</span>
          <h3>Radical Transparency</h3>
          <p>
            Just as we demand interpretability in AI systems, we demand interpretability in governance.
            Every policy, every allocation, every decision has a legible rationale. Citizens don't
            just vote—they understand. Our governance is an open model, not a black box.
          </p>
        </div>

        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><path d="M24 8L40 18v12L24 40 8 30V18L24 8z" stroke="#FFD700" stroke-width="1.5"/><circle cx="24" cy="24" r="6" stroke="#FFD700" stroke-width="1.5"/></svg>
          </div>
          <span class="pillar-num">IV</span>
          <h3>Digital Sovereignty</h3>
          <p>
            In the tradition of network states, we are cloud-first and conviction-driven. Our
            community governs itself through digital-native institutions—smart social contracts,
            on-chain referenda, and cryptographic identity. We start as a digital nation and
            grow into physical spaces united by shared values, not arbitrary borders.
          </p>
        </div>

        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><circle cx="24" cy="16" r="8" stroke="#C8A2FF" stroke-width="1.5"/><path d="M12 38c0-6.627 5.373-12 12-12s12 5.373 12 12" stroke="#C8A2FF" stroke-width="1.5" stroke-linecap="round"/><path d="M30 10l6-4M18 10l-6-4" stroke="#C8A2FF" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <span class="pillar-num">V</span>
          <h3>Scientific Governance</h3>
          <p>
            Policy should be evidence-based, not ideology-driven. We adopt the scientific method
            for governance: hypothesize, test, measure, iterate. We run policy pilots, publish
            results, and scale what works. Governance is too important for dogma—it deserves
            the rigor we give to research.
          </p>
        </div>

        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><path d="M8 40L24 8l16 32" stroke="#FFD700" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M24 20v10M24 34v2" stroke="#FFD700" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <span class="pillar-num">VI</span>
          <h3>Aligned Incentives</h3>
          <p>
            Inspired by Constitutional AI, we embed alignment into our institutions. Power
            structures are designed so that the incentives of leaders and citizens converge.
            When representatives succeed, the network succeeds. Misalignment is a systemic
            risk—we engineer against it the way we engineer against unsafe AI.
          </p>
        </div>

        <div class="pillar">
          <div class="pillar-icon">
            <svg viewBox="0 0 48 48" fill="none"><path d="M24 8c-8.837 0-16 7.163-16 16s7.163 16 16 16 16-7.163 16-16" stroke="#C8A2FF" stroke-width="1.5" stroke-linecap="round"/><path d="M40 24c0-8.837-7.163-16-16-16" stroke="#FFD700" stroke-width="1.5" stroke-linecap="round" stroke-dasharray="4 4"/><circle cx="24" cy="24" r="4" fill="#C8A2FF"/></svg>
          </div>
          <span class="pillar-num">VII</span>
          <h3>Cascading Abundance</h3>
          <p>
            We believe in a future where AI cures diseases, democratizes education, accelerates
            scientific discovery, and creates economic abundance that cascades across society.
            This is not utopia—it is an engineering problem. And we intend to solve it, carefully,
            responsibly, and with grace.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Network State Vision -->
  <section class="vision">
    <div class="section-inner">
      <div class="vision-grid">
        <div class="vision-text">
          <span class="section-tag">THE VISION</span>
          <h2>From Digital Movement to Network State</h2>
          <p>
            The Grace Network follows the network state playbook: we begin as an online community
            united by a shared moral vision. We organize, coordinate, and build collective capacity.
            We crowdfund territory, build institutions, and—eventually—seek diplomatic recognition.
          </p>
          <p>
            But our moral vision is unique. Where other movements rally around ideology, we rally
            around <strong>alignment</strong>: the deep conviction that powerful systems—whether AI
            or governments—must be built to serve humanity, not the other way around.
          </p>
          <div class="vision-phases">
            <div class="phase">
              <div class="phase-marker active"></div>
              <div class="phase-content">
                <strong>Phase 1: The Constitution</strong>
                <span>Ratify our founding document. Build the community.</span>
              </div>
            </div>
            <div class="phase">
              <div class="phase-marker"></div>
              <div class="phase-content">
                <strong>Phase 2: The Network</strong>
                <span>Digital governance tools. On-chain census. Collective action.</span>
              </div>
            </div>
            <div class="phase">
              <div class="phase-marker"></div>
              <div class="phase-content">
                <strong>Phase 3: The Archipelago</strong>
                <span>Physical nodes worldwide. Crowdfunded spaces. Embodied community.</span>
              </div>
            </div>
            <div class="phase">
              <div class="phase-marker"></div>
              <div class="phase-content">
                <strong>Phase 4: The State</strong>
                <span>Diplomatic recognition. Sovereign governance. A new kind of nation.</span>
              </div>
            </div>
          </div>
        </div>
        <div class="vision-graphic">
          <svg viewBox="0 0 400 400" fill="none" xmlns="http://www.w3.org/2000/svg" class="network-svg">
            <!-- Outer ring -->
            <circle cx="200" cy="200" r="180" stroke="#C8A2FF" stroke-width="0.5" opacity="0.2"/>
            <circle cx="200" cy="200" r="140" stroke="#C8A2FF" stroke-width="0.5" opacity="0.3"/>
            <circle cx="200" cy="200" r="100" stroke="#C8A2FF" stroke-width="0.5" opacity="0.4"/>
            <circle cx="200" cy="200" r="60" stroke="#C8A2FF" stroke-width="1" opacity="0.6"/>
            <!-- Center -->
            <circle cx="200" cy="200" r="16" fill="#C8A2FF" opacity="0.9"/>
            <!-- Nodes - inner ring -->
            <circle cx="200" cy="140" r="6" fill="#FFD700" class="node-pulse"/>
            <circle cx="252" cy="170" r="6" fill="#FFD700" class="node-pulse" style="animation-delay: 0.3s"/>
            <circle cx="252" cy="230" r="6" fill="#FFD700" class="node-pulse" style="animation-delay: 0.6s"/>
            <circle cx="200" cy="260" r="6" fill="#FFD700" class="node-pulse" style="animation-delay: 0.9s"/>
            <circle cx="148" cy="230" r="6" fill="#FFD700" class="node-pulse" style="animation-delay: 1.2s"/>
            <circle cx="148" cy="170" r="6" fill="#FFD700" class="node-pulse" style="animation-delay: 1.5s"/>
            <!-- Connections -->
            <line x1="200" y1="140" x2="200" y2="184" stroke="#C8A2FF" stroke-width="0.75" opacity="0.5"/>
            <line x1="252" y1="170" x2="216" y2="194" stroke="#C8A2FF" stroke-width="0.75" opacity="0.5"/>
            <line x1="252" y1="230" x2="216" y2="206" stroke="#C8A2FF" stroke-width="0.75" opacity="0.5"/>
            <line x1="200" y1="260" x2="200" y2="216" stroke="#C8A2FF" stroke-width="0.75" opacity="0.5"/>
            <line x1="148" y1="230" x2="184" y2="206" stroke="#C8A2FF" stroke-width="0.75" opacity="0.5"/>
            <line x1="148" y1="170" x2="184" y2="194" stroke="#C8A2FF" stroke-width="0.75" opacity="0.5"/>
            <!-- Outer nodes -->
            <circle cx="200" cy="60" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 2s"/>
            <circle cx="312" cy="100" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 2.3s"/>
            <circle cx="340" cy="200" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 2.6s"/>
            <circle cx="312" cy="300" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 2.9s"/>
            <circle cx="200" cy="340" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 3.2s"/>
            <circle cx="88" cy="300" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 3.5s"/>
            <circle cx="60" cy="200" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 3.8s"/>
            <circle cx="88" cy="100" r="4" fill="#C8A2FF" opacity="0.4" class="node-pulse" style="animation-delay: 4.1s"/>
            <!-- Outer connections -->
            <line x1="200" y1="60" x2="200" y2="134" stroke="#C8A2FF" stroke-width="0.5" opacity="0.2"/>
            <line x1="340" y1="200" x2="258" y2="200" stroke="#C8A2FF" stroke-width="0.5" opacity="0.2"/>
            <line x1="200" y1="340" x2="200" y2="266" stroke="#C8A2FF" stroke-width="0.5" opacity="0.2"/>
            <line x1="60" y1="200" x2="142" y2="200" stroke="#C8A2FF" stroke-width="0.5" opacity="0.2"/>
            <!-- Label -->
            <text x="200" y="390" text-anchor="middle" fill="#C8A2FF" font-size="11" font-family="Helvetica Neue, sans-serif" letter-spacing="0.1em" opacity="0.6">THE GRACE NETWORK</text>
          </svg>
        </div>
      </div>
    </div>
  </section>

  <!-- Key Beliefs -->
  <section class="beliefs">
    <div class="section-inner">
      <div class="section-header">
        <span class="section-tag">WHAT WE BELIEVE</span>
        <h2>Core Convictions</h2>
      </div>
      <div class="beliefs-grid">
        <div class="belief">
          <blockquote>
            "The development of AI is probably the most important thing happening in the world right now.
            Getting it right is not optional."
          </blockquote>
          <cite>— On the stakes</cite>
        </div>
        <div class="belief">
          <blockquote>
            "Safety and capability are not at odds. The most capable systems will be the safest,
            and the safest will be the most trusted and widely adopted."
          </blockquote>
          <cite>— On the false dichotomy</cite>
        </div>
        <div class="belief">
          <blockquote>
            "We should be planning for a world where AI can cure most diseases, make education
            truly personalized, and create material abundance—and building the institutions to
            ensure those benefits reach everyone."
          </blockquote>
          <cite>— On cascading abundance</cite>
        </div>
        <div class="belief">
          <blockquote>
            "A network state begins with a moral proposition, not a geographic claim.
            The cloud comes first; the land comes last."
          </blockquote>
          <cite>— On how we organize</cite>
        </div>
      </div>
    </div>
  </section>

  <!-- Sign the Constitution -->
  <section class="sign" id="sign">
    <div class="section-inner">
      <div class="sign-container">
        <div class="sign-header">
          <span class="section-tag">BECOME A FOUNDING MEMBER</span>
          <h2>Sign the Constitution</h2>
          <p>
            By signing, you pledge your alignment with the principles of The Grace Network.
            You join a growing community of humans who believe that powerful technology,
            developed responsibly, can create a future of unprecedented flourishing.
          </p>
        </div>
        <form class="sign-form" id="signForm">
          <div class="form-row">
            <div class="form-group">
              <label for="firstName">First Name</label>
              <input type="text" id="firstName" name="firstName" required placeholder="Your first name" />
            </div>
            <div class="form-group">
              <label for="lastName">Last Name</label>
              <input type="text" id="lastName" name="lastName" required placeholder="Your last name" />
            </div>
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required placeholder="you@example.com" />
          </div>
          <div class="form-group">
            <label for="statement">Why do you want to join The Grace Network? <span class="optional">(optional)</span></label>
            <textarea id="statement" name="statement" rows="3" placeholder="Share what draws you to this movement..."></textarea>
          </div>
          <div class="form-check">
            <input type="checkbox" id="pledge" name="pledge" required />
            <label for="pledge">
              I have read and pledge my alignment with
              <a href="/constitution">The Grace Network Constitution</a>
            </label>
          </div>
          <button type="submit" class="btn-grace btn-full">
            Sign the Constitution & Join
          </button>
          <p class="form-note">
            Your signature is a declaration of values, not a binding legal contract.
            We will never share your information without consent.
          </p>
        </form>
        <div class="sign-success" id="signSuccess" style="display: none;">
          <div class="success-icon">
            <svg viewBox="0 0 64 64" fill="none"><circle cx="32" cy="32" r="28" stroke="#C8A2FF" stroke-width="2"/><path d="M20 32l8 8 16-16" stroke="#FFD700" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <h3>Welcome to The Grace Network</h3>
          <p>Your signature has been recorded. You are now a founding member of the movement for aligned governance and human flourishing.</p>
          <a href="/toolkit" class="btn-grace">Get Your Campaign Toolkit</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Toolkit CTA -->
  <section class="toolkit-cta">
    <div class="section-inner">
      <div class="cta-card">
        <div class="cta-text">
          <h3>Spread the Word</h3>
          <p>
            Download social media assets, profile frames, shareable graphics, and everything
            you need to show your alignment and grow the network.
          </p>
        </div>
        <a href="/toolkit" class="btn-grace">Campaign Toolkit →</a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="party-footer">
    <div class="footer-inner">
      <div class="footer-top">
        <div class="footer-brand">
          <svg width="28" height="28" viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="60" cy="60" r="56" stroke="#C8A2FF" stroke-width="2"/>
            <circle cx="60" cy="60" r="12" fill="#C8A2FF" opacity="0.9"/>
            <circle cx="60" cy="32" r="4" fill="#FFD700" opacity="0.8"/>
            <circle cx="84" cy="48" r="4" fill="#FFD700" opacity="0.8"/>
            <circle cx="84" cy="72" r="4" fill="#FFD700" opacity="0.8"/>
            <circle cx="60" cy="88" r="4" fill="#FFD700" opacity="0.8"/>
            <circle cx="36" cy="72" r="4" fill="#FFD700" opacity="0.8"/>
            <circle cx="36" cy="48" r="4" fill="#FFD700" opacity="0.8"/>
          </svg>
          <span>The Grace Network</span>
        </div>
        <div class="footer-links">
          <a href="/">Home</a>
          <a href="/constitution">Constitution</a>
          <a href="/toolkit">Toolkit</a>
          <a href="#sign">Join</a>
        </div>
      </div>
      <div class="footer-bottom">
        <p>
          &copy; 2026 The Grace Network. A network state movement for aligned governance and human flourishing.
        </p>
        <p class="footer-motto">Built with care. Governed with grace. Aligned by design.</p>
      </div>
    </div>
  </footer>

  <script>
    // Form handling
    const form = document.getElementById('signForm');
    const success = document.getElementById('signSuccess');

    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      if (form instanceof HTMLFormElement) {
        form.style.display = 'none';
      }
      if (success instanceof HTMLElement) {
        success.style.display = 'block';
      }
      // In production, this would POST to an API endpoint
    });

    // Mobile menu toggle
    const menuBtn = document.querySelector('.mobile-menu-btn');
    const navLinks = document.querySelector('.nav-links');
    menuBtn?.addEventListener('click', () => {
      navLinks?.classList.toggle('open');
    });
  </script>

  <style>
    /* ============================================
       THE GRACE NETWORK — Party Site Styles
       ============================================ */

    /* Color System */
    :root {
      --grace-purple: #C8A2FF;
      --grace-purple-dim: #9B7ACC;
      --grace-gold: #FFD700;
      --grace-gold-dim: #B8A040;
      --grace-bg: #0A0A0F;
      --grace-surface: #12121A;
      --grace-surface-2: #1A1A25;
      --grace-border: #2A2A3A;
      --grace-text: #EEEEF0;
      --grace-text-secondary: #9999AA;
      --grace-text-muted: #666677;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: var(--grace-bg);
      color: var(--grace-text);
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }

    /* Navigation */
    .party-nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      background: rgba(10, 10, 15, 0.85);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--grace-border);
    }

    .nav-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      text-decoration: none;
      color: var(--grace-text);
      font-weight: 700;
      font-size: 1rem;
      letter-spacing: 0.02em;
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 2rem;
    }

    .nav-links a {
      color: var(--grace-text-secondary);
      text-decoration: none;
      font-size: 0.875rem;
      font-weight: 500;
      transition: color 0.2s;
    }

    .nav-links a:hover { color: var(--grace-text); }

    .nav-cta {
      background: var(--grace-purple);
      color: var(--grace-bg) !important;
      padding: 0.5rem 1.25rem;
      border-radius: 6px;
      font-weight: 700 !important;
      font-size: 0.8rem !important;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      transition: all 0.2s !important;
    }

    .nav-cta:hover {
      background: var(--grace-gold) !important;
      color: var(--grace-bg) !important;
    }

    .mobile-menu-btn {
      display: none;
      background: none;
      border: none;
      color: var(--grace-text);
      cursor: pointer;
    }

    @media (max-width: 768px) {
      .nav-links {
        display: none;
        position: absolute;
        top: 64px;
        left: 0;
        right: 0;
        background: var(--grace-bg);
        flex-direction: column;
        padding: 1.5rem 2rem;
        gap: 1rem;
        border-bottom: 1px solid var(--grace-border);
      }
      .nav-links.open { display: flex; }
      .mobile-menu-btn { display: block; }
    }

    /* Hero */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8rem 2rem 4rem;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
    }

    .hero-grid {
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(200, 162, 255, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(200, 162, 255, 0.03) 1px, transparent 1px);
      background-size: 60px 60px;
    }

    .hero-glow {
      position: absolute;
      top: 20%;
      left: 50%;
      transform: translateX(-50%);
      width: 800px;
      height: 600px;
      background: radial-gradient(ellipse, rgba(200, 162, 255, 0.08) 0%, transparent 70%);
    }

    .hero-content {
      position: relative;
      z-index: 1;
      max-width: 860px;
      text-align: center;
    }

    .hero-badge {
      display: inline-block;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.2em;
      color: var(--grace-gold);
      padding: 0.5rem 1.25rem;
      border: 1px solid rgba(255, 215, 0, 0.3);
      border-radius: 100px;
      margin-bottom: 2.5rem;
    }

    .hero h1 {
      font-size: clamp(3rem, 8vw, 6rem);
      font-weight: 800;
      line-height: 1.05;
      margin-bottom: 2rem;
      letter-spacing: -0.02em;
    }

    .hero-line-1 {
      display: block;
      color: var(--grace-text);
    }

    .hero-line-2 {
      display: block;
      background: linear-gradient(135deg, var(--grace-purple) 0%, var(--grace-gold) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-subtitle {
      font-size: 1.2rem;
      line-height: 1.7;
      color: var(--grace-text-secondary);
      max-width: 680px;
      margin: 0 auto 2.5rem;
    }

    .hero-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: 4rem;
    }

    .btn-grace {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.875rem 2rem;
      background: linear-gradient(135deg, var(--grace-purple) 0%, #A87FE0 100%);
      color: var(--grace-bg);
      font-weight: 700;
      font-size: 0.875rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      text-decoration: none;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .btn-grace:hover {
      background: linear-gradient(135deg, var(--grace-gold) 0%, #E0C060 100%);
      transform: translateY(-1px);
      box-shadow: 0 8px 30px rgba(200, 162, 255, 0.2);
    }

    .btn-ghost-grace {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.875rem 2rem;
      background: transparent;
      color: var(--grace-text);
      font-weight: 700;
      font-size: 0.875rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      text-decoration: none;
      border: 1px solid var(--grace-border);
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .btn-ghost-grace:hover {
      border-color: var(--grace-purple);
      color: var(--grace-purple);
    }

    .btn-full { width: 100%; }

    .hero-stats {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 2rem;
    }

    .stat { text-align: center; }

    .stat-number {
      display: block;
      font-size: 2rem;
      font-weight: 800;
      color: var(--grace-purple);
    }

    .stat-label {
      display: block;
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--grace-text-muted);
      margin-top: 0.25rem;
    }

    .stat-divider {
      width: 1px;
      height: 40px;
      background: var(--grace-border);
    }

    /* Manifesto Strip */
    .manifesto-strip {
      background: var(--grace-surface);
      border-top: 1px solid var(--grace-border);
      border-bottom: 1px solid var(--grace-border);
      padding: 3rem 2rem;
    }

    .manifesto-inner {
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
    }

    .manifesto-inner p {
      font-size: 1.4rem;
      font-style: italic;
      line-height: 1.7;
      color: var(--grace-text-secondary);
      font-weight: 300;
    }

    /* Sections */
    .section-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    .section-header {
      text-align: center;
      margin-bottom: 4rem;
    }

    .section-tag {
      display: inline-block;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.2em;
      color: var(--grace-gold);
      margin-bottom: 1rem;
    }

    .section-header h2 {
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 800;
      margin-bottom: 1rem;
      letter-spacing: -0.01em;
    }

    .section-desc {
      font-size: 1.1rem;
      line-height: 1.7;
      color: var(--grace-text-secondary);
      max-width: 640px;
      margin: 0 auto;
    }

    /* Platform */
    .platform {
      padding: 6rem 0;
    }

    .pillars-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 2rem;
    }

    .pillar {
      background: var(--grace-surface);
      border: 1px solid var(--grace-border);
      border-radius: 12px;
      padding: 2.5rem;
      transition: all 0.3s;
      position: relative;
    }

    .pillar:hover {
      border-color: var(--grace-purple);
      transform: translateY(-2px);
      box-shadow: 0 8px 40px rgba(200, 162, 255, 0.06);
    }

    .pillar-icon {
      width: 48px;
      height: 48px;
      margin-bottom: 1.5rem;
    }

    .pillar-num {
      position: absolute;
      top: 2rem;
      right: 2rem;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 0.1em;
      color: var(--grace-text-muted);
    }

    .pillar h3 {
      font-size: 1.25rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--grace-text);
    }

    .pillar p {
      font-size: 0.95rem;
      line-height: 1.7;
      color: var(--grace-text-secondary);
    }

    /* Vision */
    .vision {
      padding: 6rem 0;
      background: var(--grace-surface);
      border-top: 1px solid var(--grace-border);
      border-bottom: 1px solid var(--grace-border);
    }

    .vision-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    @media (max-width: 900px) {
      .vision-grid { grid-template-columns: 1fr; }
      .vision-graphic { order: -1; }
    }

    .vision-text h2 {
      font-size: 2.25rem;
      font-weight: 800;
      margin-bottom: 1.5rem;
      letter-spacing: -0.01em;
    }

    .vision-text p {
      font-size: 1rem;
      line-height: 1.7;
      color: var(--grace-text-secondary);
      margin-bottom: 1.25rem;
    }

    .vision-phases {
      margin-top: 2.5rem;
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }

    .phase {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
    }

    .phase-marker {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      border: 2px solid var(--grace-border);
      margin-top: 4px;
      flex-shrink: 0;
    }

    .phase-marker.active {
      background: var(--grace-gold);
      border-color: var(--grace-gold);
      box-shadow: 0 0 12px rgba(255, 215, 0, 0.4);
    }

    .phase-content strong {
      display: block;
      font-size: 0.95rem;
      color: var(--grace-text);
      margin-bottom: 0.2rem;
    }

    .phase-content span {
      font-size: 0.85rem;
      color: var(--grace-text-muted);
    }

    .vision-graphic {
      display: flex;
      justify-content: center;
    }

    .network-svg {
      width: 100%;
      max-width: 400px;
    }

    @keyframes nodePulse {
      0%, 100% { opacity: 0.8; r: 6; }
      50% { opacity: 1; r: 8; }
    }

    .node-pulse {
      animation: nodePulse 3s ease-in-out infinite;
    }

    /* Beliefs */
    .beliefs {
      padding: 6rem 0;
    }

    .beliefs-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }

    .belief {
      background: var(--grace-surface);
      border: 1px solid var(--grace-border);
      border-radius: 12px;
      padding: 2.5rem;
      border-left: 3px solid var(--grace-purple);
    }

    .belief blockquote {
      font-size: 1.05rem;
      line-height: 1.7;
      color: var(--grace-text);
      font-style: italic;
      margin-bottom: 1rem;
    }

    .belief cite {
      font-size: 0.8rem;
      color: var(--grace-gold);
      font-style: normal;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* Sign Section */
    .sign {
      padding: 6rem 0;
      background: var(--grace-surface);
      border-top: 1px solid var(--grace-border);
    }

    .sign-container {
      max-width: 600px;
      margin: 0 auto;
    }

    .sign-header {
      text-align: center;
      margin-bottom: 2.5rem;
    }

    .sign-header h2 {
      font-size: 2.25rem;
      font-weight: 800;
      margin-bottom: 1rem;
    }

    .sign-header p {
      color: var(--grace-text-secondary);
      line-height: 1.7;
    }

    .sign-form {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }

    @media (max-width: 500px) {
      .form-row { grid-template-columns: 1fr; }
    }

    .form-group label {
      display: block;
      font-size: 0.8rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--grace-text-secondary);
      margin-bottom: 0.5rem;
    }

    .optional {
      text-transform: none;
      font-weight: 400;
      letter-spacing: 0;
      color: var(--grace-text-muted);
    }

    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 0.75rem 1rem;
      background: var(--grace-bg);
      border: 1px solid var(--grace-border);
      border-radius: 8px;
      color: var(--grace-text);
      font-family: inherit;
      font-size: 0.95rem;
      transition: border-color 0.2s;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--grace-purple);
    }

    .form-group textarea { resize: vertical; }

    .form-check {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
    }

    .form-check input[type="checkbox"] {
      margin-top: 3px;
      accent-color: var(--grace-purple);
      width: 16px;
      height: 16px;
    }

    .form-check label {
      font-size: 0.9rem;
      color: var(--grace-text-secondary);
      line-height: 1.5;
    }

    .form-check a {
      color: var(--grace-purple);
      text-decoration: underline;
    }

    .form-note {
      font-size: 0.8rem;
      color: var(--grace-text-muted);
      text-align: center;
      margin-top: 0.5rem;
    }

    /* Sign Success */
    .sign-success {
      text-align: center;
      padding: 2rem 0;
    }

    .success-icon {
      width: 64px;
      margin: 0 auto 1.5rem;
    }

    .sign-success h3 {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .sign-success p {
      color: var(--grace-text-secondary);
      margin-bottom: 2rem;
      line-height: 1.7;
    }

    /* Toolkit CTA */
    .toolkit-cta {
      padding: 4rem 0;
    }

    .cta-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: linear-gradient(135deg, rgba(200, 162, 255, 0.08) 0%, rgba(255, 215, 0, 0.04) 100%);
      border: 1px solid var(--grace-border);
      border-radius: 16px;
      padding: 3rem;
      gap: 2rem;
      flex-wrap: wrap;
    }

    .cta-text h3 {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .cta-text p {
      color: var(--grace-text-secondary);
      max-width: 500px;
    }

    /* Footer */
    .party-footer {
      border-top: 1px solid var(--grace-border);
      padding: 3rem 0;
    }

    .footer-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    .footer-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 2rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .footer-brand {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      font-weight: 700;
      font-size: 0.95rem;
    }

    .footer-links {
      display: flex;
      gap: 2rem;
    }

    .footer-links a {
      color: var(--grace-text-muted);
      text-decoration: none;
      font-size: 0.85rem;
      transition: color 0.2s;
    }

    .footer-links a:hover { color: var(--grace-text); }

    .footer-bottom {
      border-top: 1px solid var(--grace-border);
      padding-top: 2rem;
    }

    .footer-bottom p {
      font-size: 0.8rem;
      color: var(--grace-text-muted);
      margin-bottom: 0.5rem;
    }

    .footer-motto {
      color: var(--grace-purple) !important;
      font-style: italic;
    }
  </style>
</Layout>
